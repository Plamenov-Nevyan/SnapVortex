import { SelectedFeelingDirective } from './selected-feeling.directive';

describe('SelectedFeelingDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedFeelingDirective();
    expect(directive).toBeTruthy();
  });
});
